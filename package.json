{
  "name": "ai-mv",
  "version": "0.1.0",
  "private": true,
  "description": "AI ad creative pipeline with configurable model providers and ComfyUI integration.",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "dashboard:dev": "npm --prefix dashboard run dev -- --hostname 127.0.0.1 --port 3006",
    "dashboard:build": "npm --prefix dashboard run build",
    "dashboard:start": "npm --prefix dashboard run start -- --hostname 127.0.0.1 --port 3006",
    "api:start": "node server/index.js"
  },
  "engines": {
    "node": ">=22.5"
  }
}
